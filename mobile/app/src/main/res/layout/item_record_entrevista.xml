<!-- item_record_entrevista.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/item_list_entrevista"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="377dp"
        android:layout_height="63dp"
        android:orientation="horizontal">

        <LinearLayout
            android:layout_width="245dp"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <TextView
                android:id="@+id/textViewDetEntreTitle"
                android:layout_width="230dp"
                android:layout_height="20dp"
                android:layout_marginBottom="8dp"
                android:textColor="@android:color/black"
                android:textSize="18sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/textViewDetEntreDescription"
                android:layout_width="230dp"
                android:layout_height="wrap_content"
                android:textColor="@android:color/darker_gray"
                android:textSize="14sp" />

        </LinearLayout>

        <ImageButton
            android:id="@+id/btnEditar"
            android:layout_width="35dp"
            android:layout_height="35dp"
            android:contentDescription="@string/editar_detalles"
            android:src="@android:drawable/ic_menu_edit" />

        <ImageButton
            android:id="@+id/btnVerDetalle"
            android:layout_width="35dp"
            android:layout_height="35dp"
            android:contentDescription="@string/ver_detalles"
            android:src="@android:drawable/ic_menu_view" />

        <ImageButton
            android:id="@+id/btnVerResultado"
            android:layout_width="35dp"
            android:layout_height="35dp"
            android:layout_weight="1"
            app:srcCompat="@android:drawable/ic_menu_myplaces" />

    </LinearLayout>

</LinearLayout>
