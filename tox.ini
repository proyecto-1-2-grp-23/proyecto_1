[tox]
envlist = py39
skipsdist = True
 
[testenv]
deps =
    pytest
    pytest-cov
commands = pytest -v --cov= ./backend --cov-report=xml --cov-report=html --cov-config=tox.ini --cov-branch 

[coverage:run]